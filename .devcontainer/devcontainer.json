{
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "forwardPorts": [6081, 8080],
  "portsAttributes": {
    "6081": { "label": "Session 1", "onAutoForward": "silent" },
    "8080": { "label": "Session 2", "onAutoForward": "silent" }
  },
  "postCreateCommand": "sudo apt-get install -y xvfb fluxbox x11vnc novnc websockify libgdiplus libvorbis-dev libgtk2.0-0 python3-websockify && curl -sSL https://dot.net/v1/dotnet-install.sh | bash -s -- --channel 8.0 && echo 'export DOTNET_ROOT=$HOME/.dotnet' >> ~/.bashrc && echo 'export PATH=$HOME/.dotnet:$PATH' >> ~/.bashrc"
}
