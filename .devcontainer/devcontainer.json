{
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "forwardPorts": [6081, 8080],
  "portsAttributes": {
    "6081": { "label": "Session 1", "onAutoForward": "silent", "visibility": "public" },
    "8080": { "label": "Session 2", "onAutoForward": "silent", "visibility": "public" }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y xvfb fluxbox x11vnc novnc websockify libgdiplus libvorbis-dev libgtk2.0-0 python3-websockify && curl -sSL https://dot.net/v1/dotnet-install.sh -o /tmp/dotnet-install.sh && chmod +x /tmp/dotnet-install.sh && sudo /tmp/dotnet-install.sh --channel 8.0 --runtime dotnet --install-dir /usr/share/dotnet && sudo ln -sf /usr/share/dotnet/dotnet /usr/local/bin/dotnet && dotnet --list-runtimes",
  "postStartCommand": "bash /workspaces/simcode/start_all.sh"
}
